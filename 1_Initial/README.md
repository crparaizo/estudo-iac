
vagrant init gerar aquivo de configuração da vm (Vangrantfile)

vagrant up - iniciar vm

vagrant ssh - acesso remoto na máquina virtual
exit - sair do acesso remoto

vagrant help

vangrat halt - para/shutdownd na vm

vagrant destroy - destroy máquina virtual

vagrant ssh
ip a - lista ips da máquina

sudo apt-get update 
sudo apt-get upgrade -y
sudo apt-get install apache2 -y
systemctl status apache2

ls
cd crparaizo.github.io
sudo cp -R * /var/www/html
ls /var/www/html
